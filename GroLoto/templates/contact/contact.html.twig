{% extends 'base.html.twig' %}

{% block title %}Contact - Groloto{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/contact.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
{% endblock %}

{% block body %}
<div class="hero-section">
    <div class="shine"></div>
    <div class="hero-content">
        <h1 class="hero-title">Contactez-nous</h1>
        <p class="hero-description">Une question ? Une suggestion ? Nous sommes là pour vous aider !</p>
        <a href="#contact" class="hero-btn yellow">Nous contacter</a>
    </div>
</div>
<div class="contact-container">

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endfor %}

    <div class="row justify-content-center">
        <div class="col-lg-8 col-xl-7">
            <div id="contact" class="contact-card p-4 p-md-5 shadow-lg">
                {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}

                <div class="row g-3">
                    <div class="col-md-6">
                        {{ form_label(form.nom) }}
                        {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Jean Dupont'}}) }}
                        {{ form_errors(form.nom) }}
                    </div>
                    <div class="col-md-6">
                        {{ form_label(form.email) }}
                        {{ form_widget(form.email, {'attr': {'class': 'form-control', 'placeholder': 'jean@example.com'}}) }}
                        {{ form_errors(form.email) }}
                    </div>
                </div>

                <div class="mt-3">
                    {{ form_label(form.sujet) }}
                    {{ form_widget(form.sujet, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Demande d\'information'}}) }}
                    {{ form_errors(form.sujet) }}
                </div>

                <div class="mt-3">
                    {{ form_label(form.message) }}
                    {{ form_widget(form.message, {'attr': {'class': 'form-control', 'rows': 5, 'placeholder': 'Écrivez votre message ici...'}}) }}
                    {{ form_errors(form.message) }}
                </div>

                <div class="text-center mt-4">
                    {{ form_widget(form.envoyer) }}
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}