{% extends 'base.html.twig' %}

{% block title %}Inscription Mécène{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/dashboard.css') }}">
    <link rel="stylesheet" href="{{ asset('css/auth.css') }}">
    <link rel="stylesheet" href="{{ asset('css/register.css') }}">
{% endblock %}

{% block body %}
<div class="registration-form-container">
    <div class="registration-form-card">
        <div class="registration-form-header mecene">
            <h4>Inscription Mécène</h4>
        </div>
        <div class="registration-form-body">
            {% for message in app.flashes('success') %}
                <div class="registration-alert alert-success">{{ message }}</div>
            {% endfor %}

            {% if errors %}
                <div class="registration-alert alert-danger">
                    <ul>
                        {% for error in errors %}
                            <li>{{ error }}</li>
                        {% endfor %}
                    </ul>
                </div>
            {% endif %}

            <form method="post">
                <div class="registration-row">
                    <div class="registration-col">
                        <div class="registration-form-group">
                            <label for="prenom" class="registration-form-label">Prénom *</label>
                            <input type="text" name="prenom" id="prenom" class="registration-form-control mecene-focus" required>
                        </div>
                    </div>
                    <div class="registration-col">
                        <div class="registration-form-group">
                            <label for="nom" class="registration-form-label">Nom *</label>
                            <input type="text" name="nom" id="nom" class="registration-form-control mecene-focus" required>
                        </div>
                    </div>
                </div>

                <div class="registration-form-group">
                    <label for="email" class="registration-form-label">Email *</label>
                    <input type="email" name="email" id="email" class="registration-form-control mecene-focus" required>
                </div>

                <div class="registration-form-group">
                    <label for="telephone" class="registration-form-label">Téléphone *</label>
                    <input type="tel" name="telephone" id="telephone" class="registration-form-control mecene-focus" required>
                </div>

                <div class="registration-form-group">
                    <label for="organisation" class="registration-form-label">Organisation *</label>
                    <input type="text" name="organisation" id="organisation" class="registration-form-control mecene-focus" required>
                </div>

                <div class="registration-form-group">
                    <label for="siret" class="registration-form-label">Numéro SIRET *</label>
                    <input type="text" name="siret" id="siret" 
                        class="registration-form-control mecene-focus" 
                        required 
                        pattern="^[0-9]{14}$"
                        title="Le numéro SIRET doit contenir exactement 14 chiffres">
                    <small class="registration-form-text">14 chiffres obligatoires</small>
                </div>

                <div class="registration-row">
                    <div class="registration-col">
                        <div class="registration-form-group">
                            <label for="password" class="registration-form-label">Mot de passe *</label>
                            <input type="password" name="password" id="password" class="registration-form-control mecene-focus" required minlength="6">
                            <small class="registration-form-text">Au moins 6 caractères</small>
                        </div>
                    </div>
                    <div class="registration-col">
                        <div class="registration-form-group">
                            <label for="confirm_password" class="registration-form-label">Confirmer le mot de passe *</label>
                            <input type="password" name="confirm_password" id="confirm_password" class="registration-form-control mecene-focus" required>
                        </div>
                    </div>
                </div>

                <button class="registration-submit-btn mecene" type="submit">S'inscrire comme Mécène</button>
            </form>

            <div class="registration-login-link">
                <p>Déjà un compte ? 
                    <a href="{{ path('app_login') }}" class="mecene-link">Se connecter</a>
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}