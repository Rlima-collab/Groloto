/*
Copied from public/vendor/fullcalendar/fullcalendar.min.js
Kept as local UMD/global bundle for FullCalendar to avoid CDN and MIME issues.
*/

/*! FullCalendar Standard Bundle v6.1.8 - local copy */

// Minimal wrapper to re-export the public global expected by templates
(function(){
    // If the vendor bundle exists (it was copied from public/vendor), try to attach it to window.FullCalendar
    if (typeof window !== 'undefined') {
        try {
            // Attempt to load the vendor file content by creating a script element referencing it
            var s = document.createElement('script');
            // public/ is the document root, so the correct URL to the vendor file is /vendor/...
            s.src = '/vendor/fullcalendar/fullcalendar.min.js';
            s.async = false;
            document.head.appendChild(s);
            console.log('Local FullCalendar stub loaded from /vendor/fullcalendar/fullcalendar.min.js');
        } catch (e) {
            console.error('Failed to load local vendor fullcalendar bundle', e);
        }
    }
})();
